// Auto-updater removed — the app is distributed as a local-only build.
// This component is kept as an empty placeholder.

interface UpdateDialogProps {
  isOpen: boolean;
  onClose: () => void;
}

export function UpdateDialog(_props: UpdateDialogProps) {
  return null;
}
